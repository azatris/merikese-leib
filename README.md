# Merikese Leib

Static one-page site for leib.merimeel.ee. Edit `index.html`, commit, push. GitHub Pages publishes `main`.

Photos: put image files next to `index.html`, replace the placeholder `<div class="foto">` blocks in the `#galerii` section with `<div class="foto"><img src="file.jpg" alt="..."></div>`, and remove the `hidden` attribute from that section.
